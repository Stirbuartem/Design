<Window x:Class="Design.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Design"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <Grid>
        <Border MinWidth="400" Margin="15" Background="White" VerticalAlignment="Center" Padding="30" MaxWidth="550" CornerRadius="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="45" Color="LightGray" ShadowDepth="0" />
            </Border.Effect>
            <StackPanel>
                <TextBlock HorizontalAlignment="Center" Text="Enter the login information" FontSize="25" FontWeight="Bold" Margin="0 0 0 20" />
                <TextBox    Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                <PasswordBox Style="{StaticResource MaterialDesignFloatingHintPasswordBox }" Margin="0 0 0 10"/>
                <Button Content="Sign" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
